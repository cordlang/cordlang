// ===== src/components/ListDemo.jsx =====
/* cordlang: source=src/components/ListDemo.cord */
import { createSignal } from 'solid-js';

export default function ListDemo({ title = "Items" }) {
  const [showHint, setShowHint] = createSignal(1);
  return (
    <>
      <div class="flex flex-col gap-16 p-24">
        <h1 class="text-2xl font-bold">
          {title}
        </h1>
        {showHint && (
          <>
            <p class="text-muted">
              {'Hint: click items'}
            </p>
          </>
        )}
        {(items || []).map((item, idx) => (
          <Fragment key={item.id}>
            <div class="flex flex-row gap-8 flex items-center justify-center">
              <span class="font-bold">
                {item.name}
              </span>
              <button class="btn btn-outline" onClick={() => selectItem(item.id)}>
                {'Select'}
              </button>
            </div>
          </Fragment>
        ))}
      </div>
    </>
  );
}

// ===== src/App.jsx =====
/* cordlang: source=src/app.cord */
import ListDemo from './components/ListDemo';

export default function App() {
  return <ListDemo />;
}